# object-transformer-model

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build object-transformer-model` to build the library.

## Running unit tests

Run `nx test object-transformer-model` to execute the unit tests via [Jest](https://jestjs.io).

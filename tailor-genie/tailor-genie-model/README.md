# tailor-genie-model

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tailor-genie-model` to build the library.

## Running unit tests

Run `nx test tailor-genie-model` to execute the unit tests via [Jest](https://jestjs.io).

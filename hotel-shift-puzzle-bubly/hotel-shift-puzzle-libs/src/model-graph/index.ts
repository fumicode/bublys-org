/**
 * モデルのクラス図。
 *
 * `modelGraph.generated.ts` は**自動生成**（手で編集しない）。
 * 生成し直しは bublys-libs/model-graph の cli。古くなったら
 * `modelGraph.staleness.test.ts` が落ちる。
 */
export { ModelClassDiagram } from "./ModelClassDiagram.js";
export { MODEL_GRAPH } from "./modelGraph.generated.js";

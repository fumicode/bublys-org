/**
 * モデルのクラス図。
 *
 * 構造は TypeScript のソースから生成（`modelGraph.generated.ts`）。
 * 集約の根はスライスの宣言から読む（スライスが集約のリポジトリなので）。
 * 世界線での所属は world-line の宣言（`SHIFT_TYPE` / `shiftPlanScopeId`）から。
 */
export { ModelClassDiagram } from "./ModelClassDiagram.js";
export { MODEL_GRAPH } from "./modelGraph.generated.js";

# shift-puzzle-libs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shift-puzzle-libs` to build the library.

## Running unit tests

Run `nx test shift-puzzle-libs` to execute the unit tests via [Jest](https://jestjs.io).
